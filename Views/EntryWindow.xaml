<Window x:Class="WeighbridgeSoftwareYashCotex.Views.EntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Entry - Weighbridge Software" 
        Width="800" Height="600"
        WindowStartupLocation="CenterOwner">
    
    <Border Background="White" Padding="20" BorderBrush="Gray" BorderThickness="1">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Grid.Row="0" Text="VEHICLE ENTRY" FontSize="20" FontWeight="Bold" 
                          HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Form Fields -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- RST Number -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="RST Number:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="RstNumberTextBox" IsReadOnly="True" 
                            Background="LightGray" Margin="0,5"/>

                    <!-- ID -->
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="ID:" FontWeight="Bold" VerticalAlignment="Center" Margin="20,0,10,0"/>
                    <TextBox Grid.Row="0" Grid.Column="3" x:Name="IdTextBox" IsReadOnly="True" 
                            Background="LightGray" Margin="0,5"/>

                    <!-- Vehicle Number -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Vehicle Number:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="VehicleNumberTextBox" 
                            CharacterCasing="Upper" MaxLength="10" Margin="0,5"
                            TextChanged="VehicleNumberTextBox_TextChanged"/>

                    <!-- Phone Number -->
                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Phone Number:" FontWeight="Bold" VerticalAlignment="Center" Margin="20,0,10,0"/>
                    <TextBox Grid.Row="1" Grid.Column="3" x:Name="PhoneNumberTextBox" 
                            MaxLength="10" Margin="0,5"
                            TextChanged="PhoneNumberTextBox_TextChanged"
                            PreviewTextInput="PhoneNumberTextBox_PreviewTextInput"/>

                    <!-- Name -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Name:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" x:Name="NameComboBox" 
                             IsEditable="True" Margin="0,5"
                             SelectionChanged="NameComboBox_SelectionChanged"/>

                    <!-- Address -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Address:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3" x:Name="AddressComboBox" 
                             IsEditable="True" Margin="0,5"/>

                    <!-- Material -->
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Material:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="4" Grid.Column="1" x:Name="MaterialComboBox" 
                             SelectedIndex="0" Margin="0,5"/>

                    <!-- Weight -->
                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Entry Weight:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" Margin="0,5">
                        <TextBox x:Name="WeightTextBox" Width="150" IsReadOnly="True" 
                                Background="LightYellow" FontWeight="Bold"/>
                        <TextBlock Text="KG" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        <Button Content="Capture Weight" Margin="20,0,0,0" Click="CaptureWeight_Click"/>
                    </StackPanel>

                    <!-- Date & Time -->
                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Date &amp; Time:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="6" Grid.Column="1" x:Name="DateTimeTextBox" IsReadOnly="True" 
                            Background="LightGray" Margin="0,5"/>

                    <!-- Validation Messages -->
                    <TextBlock Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="4" x:Name="ValidationMessageTextBlock" 
                              Style="{StaticResource ErrorTextStyle}" 
                              Margin="0,10" Visibility="Collapsed"/>

                    <!-- Instructions -->
                    <Border Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="4" 
                           Background="LightBlue" CornerRadius="4" Padding="10" Margin="0,20,0,0">
                        <StackPanel>
                            <TextBlock Text="Instructions:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBlock Text="• Vehicle Number: 9-10 characters (letters and digits only)" FontSize="12"/>
                            <TextBlock Text="• Phone Number: Exactly 10 digits only" FontSize="12"/>
                            <TextBlock Text="• Name and Address will auto-complete if exists in database" FontSize="12"/>
                            <TextBlock Text="• Material must be selected from dropdown" FontSize="12"/>
                            <TextBlock Text="• Use TAB or Arrow keys to navigate between fields" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" 
                           Margin="0,20">
                    <Button Content="Save Entry (F6)" x:Name="SaveButton" 
                           Style="{StaticResource WeighbridgeButtonStyle}"
                           Background="Green" Click="SaveButton_Click"/>
                    <Button Content="Clear Form (F7)" x:Name="ClearButton" 
                           Style="{StaticResource WeighbridgeButtonStyle}"
                           Background="Orange" Click="ClearButton_Click"/>
                    <Button Content="Close (Esc)" x:Name="CloseButton" 
                           Style="{StaticResource WeighbridgeButtonStyle}"
                           Background="Red" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Border>
</Window>